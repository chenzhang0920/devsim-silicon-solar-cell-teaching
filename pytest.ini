[pytest]
# Run fast unit tests by default; use `pytest -m slow` for device-simulation integration tests.
addopts = -m "not slow"
markers =
    slow: integration tests that run the full DEVSIM device model
